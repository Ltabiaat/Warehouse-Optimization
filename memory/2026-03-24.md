# 2026-03-24

## Documentation workflow
- Lancelot chose a workspace-first documentation workflow for engineering work.
- Default process: maintain structured documentation in workspace files as work progresses, then sync polished updates into Google Docs.
- Google Docs documentation should track implementation progress and function/component operations so generated code is easy to understand later.
- Official process documentation sync target: https://docs.google.com/document/d/1fTt_3JAG4_u5jb5yyL3o2rqV4ggKh2Bv497zYz1Qw5A/edit?tab=t.0

## Warehouse project direction
- ERPNext is the chosen initial WMS for the warehouse optimization project.
- Plan is to start with ERPNext and add broader capabilities later as needed.
- Real warehouse export columns were translated into an English data dictionary and normalized MVP event schema.
- Current proof-of-concept plan is to use the available movement-event dataset first, rather than waiting for perfect ERPNext or full warehouse coverage.
- Missing ideal fields are being handled with explicit workarounds so the concept can be tested cheaply and honestly.
- A first Python warehouse MVP scaffold now exists with Japanese-to-English schema mapping, row normalization logic, and passing built-in unit tests.
- The sample pipeline now runs end-to-end: sample CSV -> normalized CSV -> KPI summary JSON.
- Early signal looks good enough to keep going on the MVP: we can already extract top items, top locations, operator activity, movement mix, and expiry-related candidates.
- The warehouse Google Drive folder was imported into `docs/google-knowledge-base/` so those Docs are now available inside the repo as a local knowledge base.
- Dashboard scope was narrowed: use warehouse movement data for sales reporting and forklift tracker trajectory/state files for forklift usage reporting.
- A forklift tracker sample ingestion path now exists and generates a Power BI-ready usage summary CSV.
- A Power BI-ready sales summary CSV and dashboard specification now exist for the narrowed dashboard MVP.
- A Streamlit warehouse layout configurator scaffold now exists for warehouse size, forklift count, blocked/unreachable cell input.
- The Streamlit layout configurator now also supports named zones on the grid so reachable destinations like zone A or C can be represented.
- The Streamlit UI was then reworked into a cleaner table-style grid editor because the earlier per-cell button interaction was not working well.
- The project now has a formal conversion path from Streamlit layout JSON into canonical warehouse config, topology, and task objects for a future Gymnasium environment.
